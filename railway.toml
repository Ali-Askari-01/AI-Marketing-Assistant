[build]
builder = "nixpacks"

[deploy]
startCommand = "/bin/bash -c 'cd backend && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
