{
    "experimentalServices": {
        "web": {
            "entrypoint": "ux design/web.py",
            "routePrefix": "/",
            "buildCommand": "pip install -r ../requirements.txt"
        },
        "backend": {
            "entrypoint": "backend",
            "routePrefix": "/_/backend",
            "buildCommand": "pip install -r requirements.txt"
        }
    }
}
